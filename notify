#!/bin/bash
# feishu-relay notification wrapper
# Usage: ./notify "Title" "Message"
# Or from anywhere: /usr/local/bin/notify "Title" "Message"

DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "$DIR/run.sh" "$@"
